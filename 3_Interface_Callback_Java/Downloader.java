public class Downloader {
    // CallBack 메소드는 보통 on~ 이 붙는다.
    // OnClick, OnTouch, OnPressed 등이 붙는다.

    // 콜백을 지정한 인터페이슨느 보통
    // OnClickListener, OnTouchListener 등 Listener가 붙는다.

    public static void main(String[] args) {
        Browser broswer = new Browser();
        broswer.imgClick();
    }
}
